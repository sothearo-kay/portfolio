---
interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<body class:list={["app", className]}>
  <slot />
</body>

<style>
.app {
  width: 100%;
  max-width: 56rem;
  min-height: 100dvh;
  margin-inline: auto;
  display: grid;
  grid-template-rows: min-content 1fr min-content;
  border-inline: 1px solid var(--color-separator);

  @media (max-width: 768px) {
    border-inline: none;
  }

  main {
    min-width: 0;
  }
}
</style>
